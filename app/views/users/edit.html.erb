<h2>ユーザー編集ページ</h2>
<%= form_with model: @user, local: true do |f| %>
  <div>
    <%= f.label :name %><br />
    <%= f.text_field :name, autofocus: true %>
  </div>
  <div>
    <%= f.submit '更新'%>
  </div>
<% end %>
